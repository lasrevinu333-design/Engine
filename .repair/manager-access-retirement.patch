diff --git a/gemini-admin.html b/gemini-admin.html
index 6d11856..abf798d 100644
--- a/gemini-admin.html
+++ b/gemini-admin.html
@@ -247,0 +248,22 @@
+    [hidden] { display: none !important; }
+    .authGate {
+      min-height: 100vh;
+      display: grid;
+      place-items: center;
+      padding: max(24px, env(safe-area-inset-top)) 18px max(24px, env(safe-area-inset-bottom));
+      background:
+        linear-gradient(rgba(4, 10, 20, 0.82), rgba(4, 10, 20, 0.88)),
+        url("./Background1_optimized.webp") center / cover no-repeat fixed;
+    }
+    .authGateCard {
+      width: min(560px, 100%);
+      padding: 26px;
+      border-radius: 24px;
+      background: var(--panel-strong);
+      border: 1px solid rgba(239,68,68,.36);
+      box-shadow: var(--shadow);
+      text-align: center;
+    }
+    .authGateCard h1 { margin: 0 0 10px; font-size: 1.8rem; }
+    .authGateCard p { margin: 0; color: var(--muted); line-height: 1.5; }
+    .authGateCard .button { margin-top: 18px; }
@@ -256 +278,8 @@
-  <div class="page">
+  <section id="authGate" class="authGate" aria-live="polite">
+    <div class="authGateCard">
+      <h1>Gemini Console locked</h1>
+      <p id="authGateStatus">Verifying full-access Ops Manager authorization and Gemini password…</p>
+      <button id="authGateBack" class="button ghost" type="button">Return to Full Ops Manager Hub</button>
+    </div>
+  </section>
+  <div id="consoleApp" class="page" hidden>
@@ -362 +391 @@
-  <script src="./memphis-auth.js?v=release-2026.07.15.auth-foundation.1"></script>
+  <script src="./memphis-auth.js?v=release-2026.07.16.manager-access-repair.1"></script>
@@ -393,0 +423 @@
+      opsSession: null,
@@ -404,0 +435,4 @@
+      authGate: document.getElementById('authGate'),
+      authGateStatus: document.getElementById('authGateStatus'),
+      authGateBack: document.getElementById('authGateBack'),
+      consoleApp: document.getElementById('consoleApp'),
@@ -433,3 +467 @@
-      setStatus(`Startup error: ${safe(error)}`);
-      els.authPill.textContent = 'Auth failed';
-      els.authPill.className = 'pill bad';
+      lockConsole(error);
@@ -440 +472 @@
-      state.hub = resolveHub();
+      state.hub = 'manager';
@@ -446,0 +479,11 @@
+
+      state.opsSession = await window.MemphisAuth.requireOpsManagerSession({
+        accessLevel: 'full_access',
+        interactive: false,
+        redirect: false,
+        throwOnFailure: true
+      });
+      if (!state.opsSession || window.MemphisAuth.isReadOnlySession(state.opsSession)) {
+        throw new Error('Full-access Ops Manager authorization is required before Gemini can open.');
+      }
+
@@ -447,0 +491,2 @@
+      if (!state.session?.token) throw new Error('Gemini password authentication was not completed.');
+      unlockConsole();
@@ -456,0 +502 @@
+      els.authGateBack.addEventListener('click', goBack);
@@ -528,2 +574 @@
-      const target = state.hub === 'employee' ? './employee-hub.html' : './start_page1.html';
-      const url = new URL(target, window.location.href);
+      const url = new URL('./ops-manager-hub.html', window.location.href);
@@ -531 +576,16 @@
-      window.location.href = url.toString();
+      window.location.replace(url.toString());
+    }
+
+    function lockConsole(error) {
+      const message = safe(error) || 'Gemini Console authorization failed.';
+      if (els.consoleApp) els.consoleApp.hidden = true;
+      if (els.authGate) els.authGate.hidden = false;
+      if (els.authGateStatus) els.authGateStatus.textContent = message;
+      [els.auditBtn, els.runBtn, els.diagnoseBtn, els.refreshRuntimeBtn, els.refreshIdentityBtn, els.resetThreadBtn, els.clearBtn]
+        .filter(Boolean)
+        .forEach((button) => { button.disabled = true; });
+    }
+
+    function unlockConsole() {
+      if (els.authGate) els.authGate.hidden = true;
+      if (els.consoleApp) els.consoleApp.hidden = false;
diff --git a/memphis-auth.js b/memphis-auth.js
index 10083b7..0f98572 100644
--- a/memphis-auth.js
+++ b/memphis-auth.js
@@ -13 +12,0 @@
-  const READ_ONLY_MANAGER_ENTRY='./ops-manager-read-only.html';
@@ -43,4 +42 @@
-    try{
-      const url=new URL(window.location.href);
-      return normalizeAccessLevel(url.searchParams.get('manager_access')||url.searchParams.get('access_level')||'full_access');
-    }catch{return 'full_access';}
+    return 'full_access';
@@ -170,2 +166 @@
-  function redirectToManagerHub(accessLevel='full_access'){
-    const normalized=normalizeAccessLevel(accessLevel);
+  function redirectToManagerHub(){
@@ -173 +168 @@
-    const target=new URL(normalized==='read_only'?READ_ONLY_MANAGER_ENTRY:FULL_MANAGER_ENTRY,window.location.href);
+    const target=new URL(FULL_MANAGER_ENTRY,window.location.href);
@@ -175 +170 @@
-    target.searchParams.set('manager_access',normalized);
+    target.searchParams.set('manager_access','full_access');
@@ -198 +193 @@
-      if(redirect&&!/\/(?:start_page1|ops-manager-hub|ops-manager-read-only)\.html$/i.test(window.location.pathname||''))redirectToManagerHub(requested);
+      if(redirect&&!/\/(?:start_page1|ops-manager-hub)\.html$/i.test(window.location.pathname||''))redirectToManagerHub(requested);
@@ -202 +197 @@
-      if(redirect&&!/\/(?:start_page1|ops-manager-hub|ops-manager-read-only)\.html$/i.test(window.location.pathname||''))redirectToManagerHub(requested);
+      if(redirect&&!/\/(?:start_page1|ops-manager-hub)\.html$/i.test(window.location.pathname||''))redirectToManagerHub(requested);
diff --git a/ops-manager-hub.html b/ops-manager-hub.html
index 2904116..da90108 100644
--- a/ops-manager-hub.html
+++ b/ops-manager-hub.html
@@ -21 +21 @@
-    <p>Opening the full-access Ops Manager tools. If manager authentication is enabled, the password is entered once on this device. Employee phones never receive a password prompt.</p>
+    <p>Opening the full-access Ops Manager tools. This is the only active manager entry. If manager authentication is enabled, the password is entered once on this device. Employee phones never receive a password prompt.</p>
@@ -23 +22,0 @@
-    <a href="./ops-manager-read-only.html">Open read-only Ops Manager view</a>
@@ -25 +24 @@
-  <script src="./memphis-auth.js?v=release-2026.07.15.auth-foundation.1"></script>
+  <script src="./memphis-auth.js?v=release-2026.07.16.manager-access-repair.1"></script>
diff --git a/ops-manager-read-only.html b/ops-manager-read-only.html
deleted file mode 100644
index e07f2c8..0000000
--- a/ops-manager-read-only.html
+++ /dev/null
@@ -1,43 +0,0 @@
-<!DOCTYPE html>
-<html lang="en">
-<head>
-  <meta charset="UTF-8" />
-  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
-  <meta name="robots" content="noindex,nofollow" />
-  <title>Read-Only Ops Manager View</title>
-  <style>
-    :root{color-scheme:dark;--accent:#facc15;--panel:rgba(8,17,29,.9);--line:rgba(255,255,255,.14);--muted:#cbd5e1;--danger:#fecaca}
-    *{box-sizing:border-box}html,body{margin:0;min-height:100%;font-family:Arial,sans-serif;color:#f8fafc;background:#111827}
-    body{min-height:100vh;display:grid;place-items:center;padding:24px;background-image:linear-gradient(rgba(4,10,20,.62),rgba(4,10,20,.72)),url('./dashboard-bg_optimized.webp?v=release-2026.04.22.8');background-size:cover;background-position:center}
-    .card{width:min(520px,100%);padding:26px;border-radius:24px;background:var(--panel);border:1px solid var(--line);box-shadow:0 18px 44px rgba(0,0,0,.4);backdrop-filter:blur(8px)}
-    .eyebrow{color:var(--accent);font-size:.8rem;font-weight:900;letter-spacing:.14em;text-transform:uppercase}h1{margin:8px 0 10px;font-size:2rem;line-height:1.05}
-    p{margin:0 0 18px;color:var(--muted);line-height:1.45}.status{min-height:1.4em;color:var(--muted);font-size:.95rem;line-height:1.4}.status.error{color:var(--danger)}a{display:inline-block;margin-top:16px;color:#dff7bf;font-weight:800}
-  </style>
-</head>
-<body>
-  <main class="card">
-    <div class="eyebrow">Memphis Zoo · Read Only</div>
-    <h1>Read-Only Ops Manager View</h1>
-    <p>Opening the read-only Ops Manager tools. If manager authentication is enabled, the password is entered once on this device. Employee phones never receive a password prompt.</p>
-    <div id="status" class="status" role="status" aria-live="polite">Opening…</div>
-    <a href="./ops-manager-hub.html">Open full-access Ops Manager Hub</a>
-  </main>
-  <script src="./memphis-auth.js?v=release-2026.07.15.auth-foundation.1"></script>
-  <script>
-  (async function(){
-    'use strict';
-    const status=document.getElementById('status');
-    try{
-      const session=await window.MemphisAuth.requireOpsManagerSession({accessLevel:'read_only',interactive:true,redirect:false,throwOnFailure:true});
-      if(!session?.role)throw new Error('Manager session was not created.');
-      const target=new URL('./start_page1.html',window.location.href);
-      target.searchParams.set('manager_access','read_only');
-      window.location.replace(target.toString());
-    }catch(error){
-      status.textContent=error?.message||'Manager Hub could not open.';
-      status.className='status error';
-    }
-  })();
-  </script>
-</body>
-</html>
diff --git a/scripts/manager-open-auth-contract-tests.mjs b/scripts/manager-open-auth-contract-tests.mjs
index 7f75e0e..06b62fd 100644
--- a/scripts/manager-open-auth-contract-tests.mjs
+++ b/scripts/manager-open-auth-contract-tests.mjs
@@ -6 +5,0 @@ const full=fs.readFileSync('ops-manager-hub.html','utf8');
-const readonly=fs.readFileSync('ops-manager-read-only.html','utf8');
@@ -9,0 +9 @@ const gemini=fs.readFileSync('gemini-admin.html','utf8');
+assert.equal(fs.existsSync('ops-manager-read-only.html'),false,'read-only Ops Manager entry must be retired from GitHub Pages');
@@ -14,0 +15,3 @@ assert.match(auth,/remain trusted|only need to do this once/i);
+assert.doesNotMatch(auth,/READ_ONLY_MANAGER_ENTRY|ops-manager-read-only\.html/);
+assert.match(auth,/function redirectToManagerHub\(\)/);
+assert.match(auth,/target\.searchParams\.set\('manager_access','full_access'\)/);
@@ -17,0 +21 @@ assert.match(auth,/requireGeminiAdminSession/);
+
@@ -19 +22,0 @@ assert.doesNotMatch(full,/type=["']password|manager key/i);
-assert.doesNotMatch(readonly,/type=["']password|manager key/i);
@@ -21 +24,2 @@ assert.match(full,/accessLevel:'full_access'/);
-assert.match(readonly,/accessLevel:'read_only'/);
+assert.match(full,/only active manager entry/i);
+assert.doesNotMatch(full,/read-only Ops Manager|ops-manager-read-only/i);
@@ -23,3 +27,15 @@ assert.match(full,/password once|one time|once on this device/i);
-assert.match(readonly,/password.*once|once.*password/i);
-assert.match(hub,/requireOpsManagerSession\(\{interactive:true/);
-assert.match(gemini,/requireGeminiAdminSession/);
+
+assert.match(hub,/requireOpsManagerSession\(\{accessLevel:'full_access',interactive:true/);
+assert.match(hub,/Full-access Ops Manager authorization is required/);
+assert.doesNotMatch(hub,/Read-only manager link/);
+
+assert.match(gemini,/id="authGate"/);
+assert.match(gemini,/id="consoleApp" class="page" hidden/);
+assert.match(gemini,/requireOpsManagerSession\(\{[\s\S]*accessLevel: 'full_access'/);
+assert.match(gemini,/requireGeminiAdminSession\(\{ interactive: true \}\)/);
+assert.match(gemini,/function lockConsole\(error\)/);
+assert.match(gemini,/els\.consoleApp\.hidden = true/);
+assert.match(gemini,/Gemini password authentication was not completed/);
+assert.match(gemini,/new URL\('\.\/ops-manager-hub\.html'/);
+assert.doesNotMatch(gemini,/state\.hub === 'employee'/);
+
@@ -27,2 +43,4 @@ assert.equal((full.match(/memzoo/g)||[]).length,0,'manager entry must not embed
-assert.equal((readonly.match(/memzoo/g)||[]).length,0,'read-only manager entry must not embed a password');
-console.log('MANAGER_TRUSTED_DEVICE_AUTH_CONTRACT_PASS');
+assert.equal((gemini.match(/memzoo/g)||[]).length,0,'Gemini console must not embed a password');
+assert.match(full,/release-2026\.07\.16\.manager-access-repair\.1|manager-access-repair/);
+
+console.log('MANAGER_ACCESS_RETIREMENT_CONTRACT_PASS');
diff --git a/start_page1.html b/start_page1.html
index 08ff142..d56404d 100644
--- a/start_page1.html
+++ b/start_page1.html
@@ -121 +121 @@
-  <script src="./memphis-auth.js?v=release-2026.07.15.auth-foundation.1"></script>
+  <script src="./memphis-auth.js?v=release-2026.07.16.manager-access-repair.1"></script>
@@ -123 +123 @@
-    const CONFIG={APP_VERSION:'release-2026.07.16.foundation-repair.1',VERSION_URL:'https://memphis-zoo-mcp.onrender.com/version',ATTENDANCE_URL:'https://memphis-zoo-mcp.onrender.com/dashboard-api/current-attendance',WEATHER_URL:'https://api.open-meteo.com/v1/forecast?latitude=35.1506&longitude=-89.9944&current=temperature_2m,weather_code,wind_speed_10m&hourly=temperature_2m,precipitation_probability,weather_code&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max&timezone=America%2FChicago&forecast_days=1',DEVICE_STORAGE_KEY:'mz_scan_device_id',DEV_FALLBACK_DEVICE_ID:'1e74fe4c-dc20b3b9'};
+    const CONFIG={APP_VERSION:'release-2026.07.16.manager-access-repair.1',VERSION_URL:'https://memphis-zoo-mcp.onrender.com/version',ATTENDANCE_URL:'https://memphis-zoo-mcp.onrender.com/dashboard-api/current-attendance',WEATHER_URL:'https://api.open-meteo.com/v1/forecast?latitude=35.1506&longitude=-89.9944&current=temperature_2m,weather_code,wind_speed_10m&hourly=temperature_2m,precipitation_probability,weather_code&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max&timezone=America%2FChicago&forecast_days=1',DEVICE_STORAGE_KEY:'mz_scan_device_id',DEV_FALLBACK_DEVICE_ID:'1e74fe4c-dc20b3b9'};
@@ -130 +130 @@
-    async function init(){ initKioskLockScreen(); state.currentDeviceId=resolveDeviceId(); updateOpsLockIdentity(); let session=null; try{session=await window.MemphisAuth.requireOpsManagerSession({interactive:true,redirect:false});}catch(error){console.warn('Ops Manager access failed',error);} if(!session){showAccessRequired();return;} applyAccessMode(session); const returnPath=new URLSearchParams(window.location.search).get('return'); if(returnPath){ try { const resolved=new URL(returnPath,window.location.href); if(resolved.origin===window.location.origin && !resolved.pathname.includes('..')){ window.location.replace(resolved.toString()); } } catch(e){} return; } installImageFallbacks(); updateLinks(); startClock(); await setBuildStamp(); await Promise.allSettled([refreshWeather(),refreshAttendance()]); setInterval(refreshAttendance,30000); setInterval(refreshWeather,600000); }function applyAccessMode(session){const readOnly=window.MemphisAuth.isReadOnlySession(session);if(els.accessMode){els.accessMode.textContent=readOnly?'Read-only manager link':'Full-access Ops Manager';els.accessMode.className=`accessMode ${readOnly?'readOnly':'full'}`;}if(readOnly){[els.eventsAdminLink,els.feedbackLink,els.guestIssuesLink,els.deviceSecurityLink].filter(Boolean).forEach((link)=>{link.classList.add('readOnlyDisabled');link.setAttribute('aria-disabled','true');link.title='Read-only manager link cannot make changes.';});}}function showAccessRequired(){if(els.accessMode){els.accessMode.textContent='Manager session unavailable';els.accessMode.className='accessMode denied';}document.querySelector('.apps')?.remove();const page=document.querySelector('.page');if(page){const panel=document.createElement('section');panel.className='accessDeniedPanel';panel.innerHTML='<h2>Manager login required</h2><p>Enter the Ops Manager password once to trust this device. After enrollment, this device stays signed in until it is logged out, cleared, or revoked.</p>';page.appendChild(panel);}unlockKioskScreen();}
+    async function init(){ initKioskLockScreen(); state.currentDeviceId=resolveDeviceId(); updateOpsLockIdentity(); let session=null; try{session=await window.MemphisAuth.requireOpsManagerSession({accessLevel:'full_access',interactive:true,redirect:false,throwOnFailure:true});if(window.MemphisAuth.isReadOnlySession(session))throw new Error('Full-access Ops Manager authorization is required.');}catch(error){console.warn('Ops Manager access failed',error);} if(!session||window.MemphisAuth.isReadOnlySession(session)){showAccessRequired();return;} applyAccessMode(session); const returnPath=new URLSearchParams(window.location.search).get('return'); if(returnPath){ try { const resolved=new URL(returnPath,window.location.href); if(resolved.origin===window.location.origin && !resolved.pathname.includes('..')){ window.location.replace(resolved.toString()); } } catch(e){} return; } installImageFallbacks(); updateLinks(); startClock(); await setBuildStamp(); await Promise.allSettled([refreshWeather(),refreshAttendance()]); setInterval(refreshAttendance,30000); setInterval(refreshWeather,600000); }function applyAccessMode(){if(els.accessMode){els.accessMode.textContent='Full-access Ops Manager';els.accessMode.className='accessMode full';}}function showAccessRequired(){if(els.accessMode){els.accessMode.textContent='Manager session unavailable';els.accessMode.className='accessMode denied';}document.querySelector('.apps')?.remove();const page=document.querySelector('.page');if(page){const panel=document.createElement('section');panel.className='accessDeniedPanel';panel.innerHTML='<h2>Full-access manager login required</h2><p>This hub no longer offers a read-only entry. Enter the Ops Manager password once to trust this device for full access.</p>';page.appendChild(panel);}unlockKioskScreen();}
