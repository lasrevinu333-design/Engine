tById('attendance-meta'),employeeValue:document.getElementById('employee-value'),employeeMeta:document.getElementById('employee-meta')};
diff --git a/employee-schedule.html b/employee-schedule.html
index ef3f88c..49fca53 100644
--- a/employee-schedule.html
+++ b/employee-schedule.html
@@ -101,16 +101,16 @@
     </section>
   </div>
 
-  <script src="./memphis-auth.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
-  <script src="./memphis-device-identity.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
-  <script src="./memphis-scan-sync.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
-  <script src="./memphis-device-reminders.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
+  <script src="./memphis-auth.js?v=release-2026.07.15.schedule-messaging.5"></script>
+  <script src="./memphis-device-identity.js?v=release-2026.07.15.schedule-messaging.5"></script>
+  <script src="./memphis-scan-sync.js?v=release-2026.07.15.schedule-messaging.5"></script>
+  <script src="./memphis-device-reminders.js?v=release-2026.07.15.schedule-messaging.5"></script>
 <script>
     const CONFIG = {
       API_BASE: 'https://memphis-zoo-mcp.onrender.com/schedule-api',
       REFRESH_MS: 30000,
       DEVICE_STORAGE_KEY: 'mz_scan_device_id',
-      APP_VERSION: 'release-2026.07.15.scheduler-alerts-gps.4'
+      APP_VERSION: 'release-2026.07.15.schedule-messaging.5'
     };
     const ANNIE_RETURN_URL='https://eric-precision-tower-3620.taildaacbb.ts.net/annie';
     const ANNIE_ORIGIN_SESSION_KEY='mz_annie_origin_session';
@@ -366,15 +366,77 @@
       return normalizePurpose(item?.coverage_purpose || item?.purpose || item?.kind) === 'late_coverage';
     }
 
+    const DISPLAY_SECTION_TITLES = {
+      morning: 'Morning Full Clean Schedule',
+      rebalance: 'Restroom Rebalance Schedule',
+      lunch: '1 Hour Lunch Coverage',
+      late: 'Afternoon Call Coverage',
+      reminder: 'Reminder Only',
+      special: 'Special Coverage',
+    };
+    const DISPLAY_SECTION_ORDER = ['morning', 'rebalance', 'lunch', 'late', 'reminder', 'special'];
+
+    function displaySectionKey(item = {}) {
+      const explicit = String(item.section_key || '').trim().toLowerCase();
+      if (explicit && DISPLAY_SECTION_TITLES[explicit]) return explicit;
+      const purpose = normalizePurpose(item?.coverage_purpose || item?.purpose || item?.kind);
+      if (purpose === 'lunch_coverage') return 'lunch';
+      if (purpose === 'late_coverage') return 'late';
+      if (purpose === 'reminder') return 'reminder';
+      if (purpose === 'deep_clean') return 'morning';
+      const start = toMinutes(item?.coverage_start || item?.start_time);
+      return start >= 0 && start < 585 ? 'morning' : 'rebalance';
+    }
+
+    function uniqueText(values = []) {
+      const seen = new Set();
+      return values.map((value) => String(value || '').trim()).filter((value) => {
+        const key = value.toLowerCase();
+        if (!value || seen.has(key)) return false;
+        seen.add(key);
+        return true;
+      });
+    }
+
+    function consolidateDisplayItems(items = []) {
+      const buckets = new Map();
+      for (const item of items) {
+        const sectionKey = displaySectionKey(item);
+        const identity = String(item.location_group_id || item.group_code || itemDisplayName(item)).trim().toLowerCase();
+        const key = `${sectionKey}|${identity}`;
+        const existing = buckets.get(key) || { ...item, section_key: sectionKey, section_title: DISPLAY_SECTION_TITLES[sectionKey], included_locations: [], time_labels: [], is_current: false, source_rows: 0 };
+        existing.source_rows += Number(item.source_rows || 1);
+        existing.is_current = existing.is_current || item.is_current === true;
+        existing.included_locations.push(...(Array.isArray(item.included_locations) ? item.included_locations : []));
+        if (item.time_label) existing.time_labels.push(item.time_label);
+        else if (item.coverage_start