,currentDeviceId:'',currentRole:'',isManagerOverview:false,hubContext:'manager',threadId:'',thread:null,users:[],poller:null,lastMarkedReadSignature:'',pendingRender:false,pollInterval:2000,lastVisibleMessages:[],chatAudioCtx:null,chatAudioPrimed:false};
+const state={currentUserId:'',currentDeviceId:'',currentRole:'',isManagerOverview:false,hubContext:'manager',threadId:'',thread:null,users:[],poller:null,lastMarkedReadSignature:'',pendingRender:false,pollInterval:2000,lastVisibleMessages:[],chatAudioCtx:null,chatAudioPrimed:false,pendingClientMessageId:'',pendingClientBody:''};
 const els={backBtn:document.getElementById('back-btn'),threadAvatar:document.getElementById('thread-avatar'),threadTitle:document.getElementById('thread-title'),threadMeta:document.getElementById('thread-meta'),messages:document.getElementById('messages'),composeInput:document.getElementById('compose-input'),sendBtn:document.getElementById('send-btn'),pickerOverlay:document.getElementById('picker-overlay'),pickerList:document.getElementById('picker-list'),groupTitle:document.getElementById('group-title'),groupTitleWrap:document.getElementById('group-title-wrap'),selectAll:document.getElementById('select-all'),selectAllRow:document.getElementById('select-all-row'),pickerCancel:document.getElementById('picker-cancel'),pickerCreate:document.getElementById('picker-create'),statusPill:document.getElementById('status-pill')};
 init().catch((error)=>setStatus(`Startup error: ${safe(error)}`));
 async function init(){ const url=new URL(window.location.href); state.currentDeviceId=resolveDeviceId(url); state.currentUserId=String(url.searchParams.get('user_id')||localStorage.getItem(CONFIG.STORAGE_USER_KEY)||'').trim(); state.hubContext=resolveHubContext(url); state.threadId=String(url.searchParams.get('thread_id')||'').trim(); const mode=String(url.searchParams.get('mode')||'').trim(); bindEvents(); if(mode==='new'){ await loadUsers(); els.pickerOverlay.classList.remove('hidden'); return; } if(mode==='memphis'){ const thread=await apiPost('/memphis/thread',{user_id:state.currentUserId,device_id:state.currentDeviceId}); state.threadId=String(thread.id||'').trim(); replaceQuery({thread_id:state.threadId}); } await loadThreadMeta(); await refreshMessages(); schedulePoll(); }
@@ -62,6 +62,8 @@ async function loadUsers(){ await resolveIdentity(); state.users=await apiGet(`/
 function getParticipantCount(thread){ const names=String(thread.participant_names||'').split(',').map((value)=>value.trim()).filter(Boolean); return names.length; }
 function isCustodialTeamThread(thread){ const type=String(thread.thread_type||'').toLowerCase(); if(type!=='group'&&type!=='broadcast') return false; const rawTitle=String(thread.thread_title||thread.title||'').trim().toLowerCase(); if(rawTitle==='custodial team'||rawTitle==='everyone') return true; return getParticipantCount(thread) >= 8; }
 function getDisplayThreadTitle(thread){ if(isCustodialTeamThread(thread)) return 'Custodial Team'; return String(thread.thread_title||thread.title||'').trim() || 'Conversation'; }
+function isMemphisConversation(thread){ const title=getDisplayThreadTitle(thread).trim().toLowerCase(); return String(thread?.thread_type||'').trim().toLowerCase()==='bot'||title==='memphis'; }
+function createClientMessageId(){ try{ if(window.crypto&&typeof window.crypto.randomUUID==='function')return `msg:${window.crypto.randomUUID()}`; }catch{} return `msg:${Date.now().toString(36)}:${Math.random().toString(36).slice(2)}`; }
 async function createConversationFromPicker(){ const checked=Array.from(els.pickerList.querySelectorAll('input[type="checkbox"]:checked')).map((x)=>String(x.value||'').trim()).filter(Boolean); if(!checked.length){ setStatus('Select at least one person.'); return; } if(!state.isManagerOverview&&checked.length!==1){ setStatus('Employee devices can only start one-person conversations.'); return; } let thread; if(checked.length===1){ thread=await apiPost('/thread/direct',{created_by_user_id:sta