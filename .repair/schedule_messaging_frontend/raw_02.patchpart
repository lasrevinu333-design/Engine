 || item.coverage_end) existing.time_labels.push([item.coverage_start, item.coverage_end].filter(Boolean).join(' – '));
+        buckets.set(key, existing);
+      }
+      return Array.from(buckets.values()).map((item) => ({
+        ...item,
+        included_locations: uniqueText(item.included_locations),
+        time_label: uniqueText(item.time_labels).join(', '),
+      }));
+    }
+
     function buildSummarySections(data, items = []) {
-      const primaryItems = sortScheduleItems(items.filter((item) => !isLunchCoverageItem(item) && !isLateCoverageItem(item)));
-      const lunchItems = sortScheduleItems(items.filter(isLunchCoverageItem));
-      const lateItems = sortScheduleItems(items.filter(isLateCoverageItem));
-      const sections = [];
-      if (primaryItems.length) sections.push({ title: scheduleSectionTitle(data, primaryItems), items: primaryItems });
-      if (lunchItems.length) sections.push({ title: '1 Hour Lunch Coverage', items: lunchItems });
-      if (lateItems.length) sections.push({ title: 'Afternoon Call Coverage', items: lateItems });
-      return sections;
+      const backendSections = Array.isArray(data?.display_sections) ? data.display_sections : [];
+      if (backendSections.length) {
+        return backendSections
+          .map((section) => ({
+            key: String(section?.key || '').trim().toLowerCase(),
+            title: String(section?.title || DISPLAY_SECTION_TITLES[section?.key] || 'Schedule').trim(),
+            items: sortScheduleItems(Array.isArray(section?.items) ? section.items : []),
+          }))
+          .filter((section) => section.items.length);
+      }
+
+      const consolidated = consolidateDisplayItems(items);
+      return DISPLAY_SECTION_ORDER.map((key) => ({
+        key,
+        title: DISPLAY_SECTION_TITLES[key],
+        items: sortScheduleItems(consolidated.filter((item) => item.section_key === key)),
+      })).filter((section) => section.items.length);
     }
 
     function toMinutes(value) {
@@ -405,7 +467,7 @@
     }
 
     function scheduleItemMeta(item = {}) {
-      const locations = Array.isArray(item.included_locations) ? item.included_locations.filter(Boolean) : [];
+      const locations = uniqueText(Array.isArray(item.included_locations) ? item.included_locations : []);
       return locations.join(', ');
     }
 
diff --git a/memphis-scan-sync.js b/memphis-scan-sync.js
index 32e0c51..99d5193 100644
--- a/memphis-scan-sync.js
+++ b/memphis-scan-sync.js
@@ -9,7 +9,7 @@
     POLL_MS: 30000,
     LOCK_KEY: 'mz_scan_sync_worker_lock',
     LOCK_TTL_MS: 45000,
-    FRONTEND_VERSION: 'release-2026.07.15.scheduler-alerts-gps.4',
+    FRONTEND_VERSION: 'release-2026.07.15.schedule-messaging.5',
   };
 
   const state = {
diff --git a/messages.html b/messages.html
index 28e22ca..78a4ec3 100644
--- a/messages.html
+++ b/messages.html
@@ -70,10 +70,10 @@
   </div>
   <button id="new-chat-btn" class="fab" aria-label="New chat">+</button>
   <div id="status-pill" class="status" aria-hidden="true"></div>
-  <script src="./memphis-auth.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
-  <script src="./memphis-device-identity.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
-  <script src="./memphis-scan-sync.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
-  <script src="./memphis-device-reminders.js?v=release-2026.07.15.scheduler-alerts-gps.4"></script>
+  <script src="./memphis-auth.js?v=release-2026.07.15.schedule-messaging.5"></script>
+  <script src="./memphis-device-identity.js?v=release-2026.07.15.schedule-messaging.5"></script>
+  <script src="./memphis-scan-sync.js?v=release-2026.07.15.schedule-messaging.5"></script>
+  <script src="./memphis-device-reminders.js?v=release-2026.07.15.schedule-messaging.5"></script>
 <script>
     const MEMPHIS_AVATAR='./memphis_avatar_optimized.webp?v=release-2026.04.25.1';
     const ANNIE_RETURN_URL='https://eric-precision-tower-3620.taildaacbb.ts.net/annie';
diff --git a/script