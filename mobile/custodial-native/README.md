# Memphis Zoo Custodial Native

This is the native employee runtime. It intentionally starts with a bounded interaction foundation. Unfinished final Home capabilities are not shown as working buttons.

Build and test with the pinned Gradle wrapper after it is generated by the mission setup.
