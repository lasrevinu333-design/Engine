name: Manager Access Source Snapshot

on:
  push:
    branches: [snapshot/manager-access-retirement-20260716]

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
      - name: Archive exact frontend main
        shell: bash
        run: |
          set -euo pipefail
          git rev-parse HEAD > commit.txt
          git archive --format=tar HEAD | gzip -9 > frontend-main.tar.gz
          sha256sum frontend-main.tar.gz > frontend-main.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: manager-access-frontend-main
          path: |
            frontend-main.tar.gz
            frontend-main.sha256
            commit.txt
          if-no-files-found: error
          retention-days: 1
