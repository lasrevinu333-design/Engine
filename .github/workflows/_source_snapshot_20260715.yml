name: One-time Engine source snapshot

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Export exact source
        run: |
          git archive --format=tar.gz --output=/tmp/engine-current.tar.gz HEAD
          sha256sum /tmp/engine-current.tar.gz > /tmp/engine-current.tar.gz.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: engine-current-20260715
          path: |
            /tmp/engine-current.tar.gz
            /tmp/engine-current.tar.gz.sha256
          if-no-files-found: error
          retention-days: 1
