name: Engine Current Source Snapshot V3

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Export current source
        run: |
          git archive --format=tar.gz --output=/tmp/engine-current-v3.tar.gz HEAD
          sha256sum /tmp/engine-current-v3.tar.gz > /tmp/engine-current-v3.tar.gz.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: engine-current-source-v3
          path: |
            /tmp/engine-current-v3.tar.gz
            /tmp/engine-current-v3.tar.gz.sha256
          if-no-files-found: error
          retention-days: 1
