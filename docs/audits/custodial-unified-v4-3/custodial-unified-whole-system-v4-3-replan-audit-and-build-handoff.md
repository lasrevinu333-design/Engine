# Memphis Zoo Custodial Program — V4.3 Replan Audit and Architecture-Build Handoff

**Status:** Ready for immutable replan freeze, one focused audit, correction and standalone architecture build  
**Priority:** Custodial one-phone canary  
**Implementation authorization:** NONE

---

## 1. Purpose

This handoff avoids another six-model ceremonial reread of the entire zoo.

The v4.2 evidence cycle is complete. The next sequence is bounded:

```text
freeze replan
→ Sol High replan audit
→ correct confirmed findings
→ Terra High standalone v4.3 build
→ Spark mechanical lint
→ Sol High internal integrated audit
→ targeted independent v4.3 audits
→ final architecture decision
```

No stage starts schema, implementation, migration, APK or phone work.

---

## 2. Required inputs

Read:

1. `docs/audits/custodial-unified-v4-3/README.md`;
2. `custodial-unified-whole-system-v4-2-six-report-final-reconciliation.md`;
3. `custodial-unified-whole-system-v4-3-foundational-replan.md`;
4. `custodial-unified-whole-system-v4-3-two-week-canary-acceleration-plan.md`;
5. the immutable v4.2 architecture package at `be01c7b382da14e0e98375ee7a03e88c26ee598c`;
6. exact source only where needed to verify a disputed replan statement.

Do not rediscover settled product doctrine or rerun all six v4.2 reports.

The mechanical preflight controls:

- main CAP row cardinality;
- shorthand resolution;
- section resolution;
- gate resolution;
- package paths/hashes;
- frozen branch/commit equality.

A replan auditor may challenge semantic trace/object completeness but may not revive the false duplicate-CAP blocker without a narrower deterministic counter-proof.

---

## 3. Stage A — Sol High replan audit

### Mission

Audit the **reconciliation and replan**, not v4.2 from scratch.

Determine whether every accepted v4.2 finding has:

- correct root cause;
- correct architecture layer;
- exact closure artifact;
- correct proof and gate;
- no hidden patch or compatibility authority;
- no unnecessary whole-program expansion;
- no contradiction with retained product doctrine;
- no missing dependency in the fourteen-day canary plan.

### Required output

One report containing:

1. access and exact freeze evidence;
2. `GO`, `CONDITIONAL GO` or `NO-GO` for writing standalone v4.3;
3. confirmed/rejected/incomplete replan findings;
4. missing artifacts or fields;
5. cross-artifact contradictions;
6. architecture-build instructions;
7. prioritized corrections;
8. exit gate for Stage B;
9. no-change statement.

### Token boundary

- Use Sol at High.
- Do not use Ultra unless one exact contradiction remains unresolved after primary evidence inspection.
- Do not produce implementation code or DDL.

---

## 4. Stage B — Replan correction

The architecture lead:

- accepts findings only against primary evidence;
- corrects the replan package;
- records rejected findings and reasons;
- reruns the narrow replan checks;
- freezes the corrected replan.

If Stage A finds no BLOCKER, correction may be completed in the same architecture worktree. Material changes receive a targeted Sol recheck rather than a full reread.

---

## 5. Stage C — Terra High standalone v4.3 build

### Mission

Write one standalone Unified Whole-System Architecture v4.3 and its machine-readable control artifacts.

V4.3 may import validated v4.2 doctrine but must not require a reader to combine v4.2 plus addenda to discover the governing architecture.

### Mandatory output artifacts

1. `custodial-unified-whole-system-architecture-v4-3.md`;
2. package manifest and stage-state schema;
3. architecture-object registry;
4. complete joined CAP trace v3 plus generated Markdown/CSV;
5. complete reverse registry;
6. record-type registry;
7. principal/service-principal/grant/tool registry;
8. authority-set registry, transition model and compatibility matrix;
9. gate registry and design-impact matrix;
10. offline/session/completion transition model;
11. occurrence command/state model;
12. location-transition model;
13. legacy authority retirement manifest;
14. rollback/restore bundle contract;
15. communication-classification matrix;
16. NFC recovery contract;
17. contractor failure lifecycle;
18. notification accessibility contract;
19. purge/restore ledger contract;
20. v17 screenshot disposition ledger;
21. architecture validation script/specification;
22. updated programmer and independent-audit handoffs.

### Build rules

- documentation and registries only;
- no DDL or implementation;
- no invented operating value;
- exact engineering mechanics are not deferred to Eric;
- structure-changing policy remains gated;
- optional Guest, Weather, Moxie and AI-write capabilities remain disabled/future unless legitimate current purpose is approved;
- anonymous full MCP mode is `RETIRE`;
- Build 22 remains `RETAIN-UNPROVEN` until physical rollback;
- every adapter is bounded, observable and retirement-dated.

---

## 6. Stage D — Spark mechanical validation

Spark High performs deterministic checks only:

- manifest/path/hash integrity;
- artifact schema validation;
- CAP-001–CAP-252 exactly once;
- all mandatory trace fields present;
- all object/record/principal/gate/evidence/proof references resolve;
- no orphan object;
- no CAP without exact authority/record/writer/resolver/proof;
- no contradictory stage authorization;
- no structure-changing gate mislabeled value-only;
- generated Markdown/CSV match source JSON;
- all accepted v4.2 findings map to closure artifacts.

Spark does not issue the final integrated architecture verdict.

---

## 7. Stage E — Sol High internal integrated audit

Sol tests:

- cross-domain compatibility;
- original-actor offline identity;
- authority-set activation/rollback;
- service-principal and tool security;
- occurrence/location concurrency;
- migration retirement completeness;
- rollback/restore ordering;
- operations/Karen/product doctrine;
- two-week dependency correctness;
- false-confidence tests.

Required verdict:

- `GO for targeted independent architecture audits only`;
- `CONDITIONAL GO` with bounded corrections; or
- `NO-GO`.

Schema and implementation remain closed.

---

## 8. Stage F — Targeted independent audits

Do not automatically rerun four enormous whole-repository audits.

Use targeted independent first passes:

1. **Spark mechanical/security audit** — registries, transitions, migration/retirement, false-confidence tests;
2. **GPT-5.5 Instant capability audit** — trace/object completeness, hidden omissions and evidence drift;
3. **GPT-5.5 Pro operations audit** — Karen, manager, contractor, Dashboard and physical-plan contracts;
4. **GPT-5.6 Pro integrated audit** — authority, identity, security, rollback, migration and cross-domain compatibility.

Each auditor reads v4.3 first and inspects source only where needed. It does not repeat v17/source reconstruction already frozen unless a v4.3 claim depends on it.

All final reports remain one fenced copyable block beginning with the exact model name.

---

## 9. Final architecture gate

V4.3 may authorize isolated schema/component design only if:

- mechanical validation passes;
- no unresolved architecture BLOCKER remains;
- every structure-changing gate needed for the affected design is closed;
- all accepted v4.2 findings have verified closure;
- no later designer must invent foundational semantics;
- Build 22 and the physical acceptance plan remain preserved;
- final reconciliation explicitly opens only the appropriate design gates.

Architecture GO does not authorize implementation.

---

## 10. Codex autonomy and safety

For these stages:

- reversible, mission-scoped work proceeds without user confirmation;
- read-only audits use `approval_policy="never"` and `sandbox="read-only"`;
- documentation work uses an isolated worktree and `sandbox="workspace-write"`;
- no force push or shared-history rewrite;
- no production writes;
- no spending;
- irreversible destruction requires Eric;
- every output is committed, hashed and attached to its stage record.

---

## 11. Progress reporting

Athanor/controller state must distinguish:

- `JOB_STARTED`;
- `JOB_RUNNING`;
- `JOB_FINISHED`;
- `REPORT_EXTRACTED`;
- `REPORT_VERIFIED`;
- `REPORT_DELIVERED`;
- `NEXT_STAGE_READY`;
- `NEXT_STAGE_BLOCKED`.

A later stage being blocked must never reclassify a finished prior job as unfinished.

---

## 12. No-change statement

This handoff authorizes only architecture documentation, read-only research, isolated registry generation, validation and audit.

---

## 13. V4.3.1 corrected handoff (controlling)

Stage B now consumes the exact canonical contracts under `contracts/` named in the foundational replan §17. Stage B exit requires all deterministic validators, focused internal adversarial review, immutable content freeze, detached attestation, and targeted GPT-5.6 Sol High recheck.

Stage C mandatory inputs are expanded to include, as canonical JSON rather than fixtures or prose promises:

1. immutable content manifest, detached attestation reference, and append-only stage-control model;
2. accepted-finding closure registry with exactly 22 active V43-B/H rows;
3. artifact field-ownership and generation DAG;
4. credential-type, session-type, grant, authorization-decision, executable-tool, service-principal, and MCP plane-boundary contracts;
5. authority-set activation/rollback and restore-bundle command/state contracts;
6. original-actor authorization plus occurrence and location transition contracts;
7. notification lifecycle/provider reconciliation;
8. active-session GPS authority;
9. Messenger visibility/reappearance/hold/restore;
10. Event revision/impact/reversal concurrency;
11. readiness/severity/inspection/reopen;
12. product-boundary and build-graph exclusions;
13. complete gate dependencies and generated workstream/day projection.

Model routing is capability authority, not a product-name promise: correction requires a balanced architecture model at High reasoning; the independent targeted acceptance recheck is GPT-5.6 Sol High. Any unavailable named model blocks or receives an explicit recorded reroute; it is never silently substituted.

Day 11 restore work is rehearsal. Final rollback/restore proof is repeated after Day 12 against the exact signed canary tuple. “Fourteen implementation days after Day-0 authorization, with pre-Day-1 closure outside that clock” is the only schedule term.

Stage B and this handoff authorize documentation, validation, remote evidence publication, and audit only. All schema, product implementation, production, migration, APK, phone, canary, and release authority remains closed.

