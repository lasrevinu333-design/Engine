# Custodial Unified v4.3 corrected foundational replan — correction v1

This canonical documentation/control overlay supersedes conflicting package identity, stage authority, accepted-finding coverage, security-plane, transition, gate-dependency, and schedule language in the originating v4.3 replan without reopening settled product doctrine.

Originating working branch: `agent/custodial-unified-whole-system-v4-3-20260805` at `82cff733980b45f9e1af9e579a90c5b69e5d2307`. Unchanged prior freeze: `audit/custodial-unified-v4-3-replan-freeze-20260805` at `58785877db4a5ce44cee18501e2294d473a05467`.

The immutable content manifest excludes itself. A detached private attestation binds its digest to the exact corrected tree and commit. Stage and gate decisions are append-only detached records; prose status never authorizes work. Generated projections are byte-stable and non-canonical.

The schedule term is exactly **fourteen implementation days after Day-0 authorization**. Pre-Day-1 closure is outside that clock. Day 11 is rehearsal; final rollback/restore proof repeats after the exact Day-12 signed tuple and before canary.

No frontend, backend, schema, migration, deployment, APK, device, NFC tag, or production configuration is changed.
