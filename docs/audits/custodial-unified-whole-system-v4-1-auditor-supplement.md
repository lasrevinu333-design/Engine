# Memphis Zoo Custodial Program — v4.1 Auditor Evidence Supplement

**Applies to:** all four prompts in `custodial-unified-whole-system-v4-1-auditor-prompt-pack.md`

Before completing the independent first pass, also read:

- `docs/audits/custodial-unified-whole-system-production-truth-addendum-v3.md`
- `docs/audits/custodial-unified-whole-system-production-truth-addendum-v4.md`

Add these mandatory attacks:

1. Enumerate and dispose of every deployed schedule-writer family, including legacy generation, rolling cron, absence triggers, lunch/restored-scan mutators, location-template seeding, SCH2 preview/publish/rollback, API routes and AI/message-triggered generation.
2. Determine whether v4.1 provides a sufficient migration/retirement contract so no unlinked legacy function remains callable after cutover.
3. Audit all employee-area preferences and named-person/employee-code special cases as hard restrictions, soft preferences, accommodations, position rules, temporary exceptions or obsolete fixtures.
4. Reject free-text notes or person-named SQL functions as canonical compiler policy.
5. Protect confidential accommodation justification from employee Schedule, ordinary manager explanation, analytics and exports while retaining an auditable approved operational effect.
6. Verify generic versioned policy/validator structures can replace named Alijah/Kathy/gift-shop functions without losing valid operational rules or keeping historical employees artificially active.
7. Treat SCH2 as an adversarial comparison, not an approved target. It has valuable preview/diff/locking/snapshot patterns but still solves group-level work, uses incomplete MD5/JSON inputs, re-optimizes valid static ownership, contains person/group hard-coded policy, deletes/replaces the whole day and rolls back only legacy daily rows.
8. Verify v4.1 correctly permits truthful published `OPEN`; do not inherit SCH2's rule that any required `OPEN` blocks publication.
9. Confirm canonical publication/rollback includes ownership, service occurrence/status, transition evidence, notification intents, client compatibility and authority pointer—not just daily assignment bytes.
10. Confirm the target input fingerprint includes the full compatible authority set and uses the versioned canonical serialization/SHA-256 contract.

Do not treat stable employee UUID matching as proof that a person-bound rule is correctly modeled. The question is which entity and policy the rule legitimately belongs to.

Do not treat transactional care as proof that a scheduler uses the correct authority unit or operating model.